# Зависимости для OCR постобработки (ocr_postprocessor.py)
# Установка: pip install -r ocr_requirements.txt

# Обязательные библиотеки
ftfy>=6.1.0                    # Исправление кодировочных артефактов
regex>=2023.0.0                # Расширенные регулярные выражения
rapidfuzz>=3.0.0               # Быстрая нечеткая строковая подстановка
pymorphy3>=1.2.0               # Морфологический анализ русского языка
pymorphy3-dicts-ru>=2.4.0      # Словари для pymorphy3

# Опциональные библиотеки (рекомендуемые)
razdel>=0.5.0                  # Токенизация с сохранением пробелов
confusable-homoglyphs>=3.2.0   # Замена омографов (unicode homoglyphs)

